import { Card } from "../components/Card";

export default function Events() {
  return (
    <Card title="Events">
      <p className="text-slate-500 text-sm">この画面は実装予定です（Phase1/2）。</p>
    </Card>
  );
}
